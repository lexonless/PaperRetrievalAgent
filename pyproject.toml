[project]
name = "paper-retrieval-agent"
version = "0.1.0"
description = "A LangGraph-based research assistant for project-centered paper retrieval and research notes."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115,<1.0",
    "langchain-core>=0.3",
    "langchain-openai>=0.3",
    "langgraph>=0.2",
    "httpx>=0.27,<1.0",
    "markdown-it-py>=3.0,<4.0",
    "pydantic>=2.8,<3.0",
    "pypdf>=5.0,<6.0",
    "python-dotenv>=1.0,<2.0",
    "uvicorn>=0.30,<1.0",
]

[project.scripts]
paper-agent = "paper_research_agent.main:main"
paper-research-web = "paper_research_agent.langgraph_web:main"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
