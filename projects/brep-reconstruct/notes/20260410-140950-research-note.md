# Tentative Survey: Neural B-Rep Reconstruction from Point Clouds (2021-2026)

**Project:** `brep-reconstruct`
**Query:** **Role & Task:**
Act as an expert 3D computer vision and geometric deep learning researcher. Your task is to search for and analyze 8-10 state-of-the-art frontier research papers (published between 2021-2026) focused explicitly on 3D Boundary Representation (B-Rep) reconstruction and generation. 

**Primary Focus (What to INCLUDE):**
* **Target Output:** The primary output of the model MUST be the B-Rep data structure itself (faces, edges, vertices, and their topological relationships).
* **Target Input:** Unstructured Point Clouds or 3D scans are the highest priority. Multimodal or sketch/image inputs are acceptable only if the method is a major breakthrough in B-Rep generation.
* **Methodology:** Highly prioritize modern neural architectures, including latent space representations, variational approaches, or end-to-end direct neural generation of continuous B-Rep geometry and topology.

**CRITICAL EXCLUSIONS (What to strictly AVOID):**
You MUST STRICTLY EXCLUDE any paper that falls into these categories:
1.  **NO CAD Operation Sequences:** Do not include papers where the goal is to reconstruct CAD construction trees, sketch-and-extrude histories, or sequential modeling steps (e.g., DeepCAD, Sketex).
2.  **NO CSG Trees:** Exclude papers focusing on Constructive Solid Geometry (CSG) primitive combinations.
3.  **NO Program Synthesis:** Exclude papers that generate CAD programs or macros.
*If the model outputs a step-by-step modeling history rather than directly outputting the final B-Rep topological/geometric data structure, it is a hard fail. Discard it.*

**Output Format:**
For each of the 8-10 selected papers, provide:
* **Title & Year:**
* **Source/Venue:** (e.g., CVPR, SIGGRAPH, NeurIPS)
* **Link:**
* **Core Contribution:** (1-2 sentences on their specific technical breakthrough)
* **Relevance Note:** (Explain exactly how it tackles direct B-Rep generation from point clouds, and confirm it does not rely on step-by-step operation sequences).

**Synthesis Report:**
After listing the papers, provide a concise summary covering:
1.  **Major Method Categories:** (How are current models representing B-Rep natively for neural networks?)
2.  **Recent Trends:** (e.g., shifts in handling topology vs. geometry)
3.  **Open Challenges:** (Specific bottlenecks in point-cloud-to-B-Rep tasks)
**Generated At:** 2026-04-10T14:09:50

## Project Focus

Identifying state-of-the-art neural methods that directly generate Boundary Representation (B-Rep) data structures (faces, edges, vertices, topology) from unstructured point clouds or 3D scans, excluding CAD operation sequences, CSG trees, and program synthesis.

## Retrieval Assessment

Retrieval was broad but imprecise, returning many weakly relevant or off-topic papers. The reranking process failed to produce usable relevance judgments. Consequently, no papers have been verified as meeting all core criteria (point cloud input, direct B-Rep output). The evidence base is thin and unconfirmed.

## Search Scope

- neural B-Rep reconstruction from unorganized point clouds
- learning boundary representations from 3D scans deep learning
- differentiable B-Rep extraction from point cloud
- topology-aware point cloud reconstruction to B-Rep
- direct boundary representation generation neural network
- point cloud to B-Rep end-to-end neural network

## Core Papers

_No core papers were selected._

## Supporting Papers

### 1. Point2Brep: Geometry-Aware B-rep Reconstruction from Point Clouds (2026)

- **Source:** `SSRN`
- **Link:** https://doi.org/10.2139/ssrn.6172545
- **Contribution:** Proposes a two-stage framework integrating learning-based surface patch segmentation with geometric reasoning via predicted distance fields to recover edges, vertices, and connectivity.
- **Why It Matters:** Explicitly targets B-rep reconstruction from noisy point clouds. Mentions a learning-guided stage but the full pipeline integrates explicit geometric reasoning. Input is point clouds.
- **Evidence:** Evidence from abstract confirms point cloud input and B-rep output goal, but the neural architecture's role versus explicit modeling is unclear. Not fully verified.

### 2. BrepGPT: Autoregressive B-rep Generation with Voronoi Half-Patch (2025)

- **Source:** `arXiv`
- **Link:** http://arxiv.org/abs/2511.22171v1
- **Contribution:** Proposes a single-stage autoregressive Transformer framework using a novel Voronoi Half-Patch (VHP) representation and dual VQ-VAEs to unify geometry and topology in a sequential format.
- **Why It Matters:** Designed for B-rep generation. The abstract lists conditional generation from point clouds as an application, suggesting point cloud input is supported.
- **Evidence:** Abstract mentions conditional generation from point clouds, but the primary focus appears to be unconditional generation. The method's handling of point cloud input is not detailed in the snippet.

### 3. AutoBrep: Autoregressive B-Rep Generation with Unified Topology and Geometry (2025)

- **Source:** `arXiv`
- **Link:** http://arxiv.org/abs/2512.03018v1
- **Contribution:** Presents a Transformer model that autoregressively generates B-Reps using a unified tokenization scheme encoding both geometric primitives and topological references.
- **Why It Matters:** Targets direct B-Rep generation with precise geometry and watertight topology. Input modality is not specified in the provided evidence.
- **Evidence:** Abstract does not mention input type (point cloud or otherwise). Relevance to point-cloud-to-B-Rep task is unconfirmed.

### 4. AutoRegressive Generation with B-rep Holistic Token Sequence Representation (2026)

- **Source:** `arXiv`
- **Link:** http://arxiv.org/abs/2601.16771v2
- **Contribution:** Proposes BrepARG, encoding B-rep geometry and topology into a holistic token sequence to enable autoregressive generation with a transformer.
- **Why It Matters:** Focuses on a novel sequence representation for B-reps to enable autoregressive generation. Input modality is not discussed in the snippet.
- **Evidence:** Abstract is silent on input. Paper may be about representation/generation in general, not specifically reconstruction from point clouds.

## Local Context

- Project 'brep-reconstruct' aims to explore direct neural generation of B-Reps from point clouds.
- Hard exclusions: CAD operation sequences, CSG trees, program synthesis.

## Key Observations

- The retrieval was noisy, indicating the search niche (neural, point-cloud-to-B-Rep) is very specific and possibly small.
- Several recent (2025-2026) papers propose autoregressive transformer models for B-Rep generation (BrepGPT, AutoBrep, BrepARG), suggesting a trend towards sequence-based, unified representations.
- One paper (Point2Brep) explicitly mentions a hybrid learning-and-geometric-reasoning pipeline for reconstruction from point clouds.
- No paper's evidence snippet conclusively confirms a fully neural, end-to-end pipeline from raw point clouds to a final, topologically valid B-Rep structure.

## Open Questions

- What are the dominant neural architectures for representing B-Rep topology natively? (Graphs vs. sequences vs. implicit fields)
- Is the field converging on autoregressive/transformer-based methods for joint topology-geometry generation?
- What are the specific bottlenecks in achieving watertight topology directly from noisy, unorganized point clouds?
- How do state-of-the-art methods quantitatively compare on standard point-cloud-to-B-Rep benchmarks?

## Limitations

- This note is tentative due to failed retrieval verification and exhausted revision budget.
- Core papers are not verified; relevance and method details are inferred from abstracts only.
- Supporting papers may not fully meet the project's strict input/output criteria.
- Critical coverage gaps exist; likely key state-of-the-art papers from top venues (CVPR, SIGGRAPH) are missing.
- Synthesis of major method categories, trends, and challenges cannot be credibly performed with the current evidence.

## Next Steps

- Revise search strategy with sharper queries and mandatory exclusion terms (e.g., -CSG, -"operation sequence", -"program synthesis").
- Perform venue-specific searches (CVPR, SIGGRAPH, NeurIPS) for "B-Rep reconstruction".
- Manually inspect the full text of candidate papers (Point2Brep, BrepGPT) to verify input/output alignment.
- Search for known benchmark datasets (e.g., ABC dataset) to find associated reconstruction papers.
- Consider backward/forward citation chaining from the most promising candidates to build a more reliable literature set.
