# Tentative Survey: Direct B-Rep Reconstruction and Generation (2021-2026)

**Project:** `brep-reconstruct`
**Query:** **Role & Task:**
Act as an expert 3D computer vision and geometric deep learning researcher. Your task is to search for and analyze 8-10 state-of-the-art frontier research papers (published between 2021-2026) focused explicitly on 3D Boundary Representation (B-Rep) reconstruction and generation. 

**Primary Focus (What to INCLUDE):**
* **Target Output:** The primary output of the model MUST be the B-Rep data structure itself (faces, edges, vertices, and their topological relationships).
* **Target Input:** Unstructured Point Clouds or 3D scans are the highest priority. Multimodal or sketch/image inputs are acceptable only if the method is a major breakthrough in B-Rep generation.
* **Methodology:** Highly prioritize modern neural architectures, including latent space representations, variational approaches, or end-to-end direct neural generation of continuous B-Rep geometry and topology.

**CRITICAL EXCLUSIONS (What to strictly AVOID):**
You MUST STRICTLY EXCLUDE any paper that falls into these categories:
1.  **NO CAD Operation Sequences:** Do not include papers where the goal is to reconstruct CAD construction trees, sketch-and-extrude histories, or sequential modeling steps (e.g., DeepCAD, Sketex).
2.  **NO CSG Trees:** Exclude papers focusing on Constructive Solid Geometry (CSG) primitive combinations.
3.  **NO Program Synthesis:** Exclude papers that generate CAD programs or macros.
*If the model outputs a step-by-step modeling history rather than directly outputting the final B-Rep topological/geometric data structure, it is a hard fail. Discard it.*

**Output Format:**
For each of the 8-10 selected papers, provide:
* **Title & Year:**
* **Source/Venue:** (e.g., CVPR, SIGGRAPH, NeurIPS)
* **Link:**
* **Core Contribution:** (1-2 sentences on their specific technical breakthrough)
* **Relevance Note:** (Explain exactly how it tackles direct B-Rep generation from point clouds, and confirm it does not rely on step-by-step operation sequences).

**Synthesis Report:**
After listing the papers, provide a concise summary covering:
1.  **Major Method Categories:** (How are current models representing B-Rep natively for neural networks?)
2.  **Recent Trends:** (e.g., shifts in handling topology vs. geometry)
3.  **Open Challenges:** (Specific bottlenecks in point-cloud-to-B-Rep tasks)
**Generated At:** 2026-04-08T19:27:02

## Project Focus

Identifying state-of-the-art neural methods for directly generating 3D Boundary Representation (B-Rep) data structures (faces, edges, vertices, topology) from unstructured inputs like point clouds or 3D scans, excluding CAD operation sequences, CSG trees, and program synthesis.

## Retrieval Assessment

FAILED. The retrieval was highly insufficient. Only one paper was directly relevant to the core query. The search failed to capture foundational works (e.g., BRepNet 2021) and recent point-cloud-to-B-Rep reconstruction papers. Most results were weakly related (LLM-driven CAD program synthesis, rotation-invariant learning) or completely off-topic.

## Search Scope

- "B-Rep" "point cloud" reconstruction
- "neural B-Rep" generation
- "boundary representation" deep learning
- CVPR, SIGGRAPH, NeurIPS venues (2021-2026)

## Core Papers

### 1. GraphBRep : Explicit Graph Diffusion of B–Rep Topology for Efficient CAD Generation (2025)

- **Source:** `Journal of Computational Design and Engineering`
- **Link:** https://doi.org/10.1093/jcde/qwaf136
- **Contribution:** Proposes a graph diffusion model that explicitly learns compact B-Rep surface topology conditioned on surface features to determine connectivity between primitive surfaces, aiming for efficient generation.
- **Why It Matters:** Directly addresses unconditional and category-conditional B-Rep generation. The abstract emphasizes direct generation and does not mention operation sequences.
- **Evidence:** Abstract states the method is for 'Direct B-Rep generation' and uses a 'graph diffusion model' to learn topology.

## Supporting Papers

### 1. AutoRegressive Generation with B-rep Holistic Token Sequence Representation (2026)

- **Source:** `arXiv`
- **Link:** http://arxiv.org/abs/2601.16771v2
- **Contribution:** Proposes BrepARG, encoding B-rep geometry and topology into a holistic token sequence for autoregressive generation with a transformer.
- **Why It Matters:** Focuses on direct B-Rep generation from a learned sequence distribution. Input modality is not specified in the abstract; relevance to point clouds is unclear.
- **Evidence:** Abstract describes a 'holistic token sequence representation' for 'sequence-based B-rep generation'.

### 2. AutoBrep: Autoregressive B-Rep Generation with Unified Topology and Geometry (2025)

- **Source:** `arXiv`
- **Link:** http://arxiv.org/abs/2512.03018v1
- **Contribution:** Presents a Transformer model that autoregressively generates B-Reps using a unified tokenization scheme for geometry and topology.
- **Why It Matters:** Direct B-Rep generation method. The abstract does not specify the input, so its applicability to point cloud reconstruction is not confirmed.
- **Evidence:** Abstract states the model 'autoregressively generates B-Reps' with a 'unified tokenization scheme'.

### 3. BrepGPT: Autoregressive B-rep Generation with Voronoi Half-Patch (2025)

- **Source:** `arXiv`
- **Link:** http://arxiv.org/abs/2511.22171v1
- **Contribution:** Introduces a single-stage autoregressive framework using a Voronoi Half-Patch representation and dual VQ-VAEs for B-rep generation.
- **Why It Matters:** Describes a direct B-Rep generation framework. The abstract mentions conditional generation from point clouds, but the core method is not specifically a reconstruction-from-scan model.
- **Evidence:** Abstract lists 'conditional generation from... point clouds' as an application, but the primary contribution is the generative framework.

## Local Context

- Project 'brep-reconstruct' aims to understand neural methods for creating B-Reps from raw 3D data.
- Initial hypothesis: A nascent but active subfield exists, with key papers from major vision/graphics conferences.

## Key Observations

- 1. The retrieved set is dominated by autoregressive, token sequence-based generative models for B-Reps (BrepARG, AutoBrep, BrepGPT).
- 2. Graph-based representation and diffusion models (GraphBRep) are also present.
- 3. A significant portion of recent 'CAD generation' literature focuses on LLM-driven program synthesis or command sequences, which is outside our defined scope.
- 4. No retrieved paper explicitly demonstrates a neural network reconstructing a B-Rep from an unstructured point cloud or 3D scan, which was the primary focus of the query.

## Open Questions

- What are the foundational neural architectures for B-Rep representation (e.g., BRepNet 2021) and how have they evolved?
- Are there any published works on end-to-end neural reconstruction of B-Rep topology and geometry from point clouds or scans?
- How do current direct generation methods (autoregressive, diffusion) handle the challenge of watertightness and topological validity?
- What datasets are used for training and evaluating direct B-Rep generation models?

## Limitations

- This note is highly tentative due to failed retrieval. The core literature is missing.
- The single core paper (GraphBRep) may not address the specific input (point clouds) of interest.
- Supporting papers are generative models whose input conditions and relevance to reconstruction are unclear.
- No analysis of methodological categories, trends, or challenges is possible with the current evidence.

## Next Steps

- Conduct a revised, targeted search using citation chaining from the foundational paper 'BRepNet: A topological message passing system for solid models' (CVPR 2021).
- Execute new queries: 'point cloud to boundary representation reconstruction neural network', 'scan-to-B-Rep deep learning', 'neural reconstruction of B-Rep from point cloud'.
- Explicitly exclude LLM and program synthesis terms in queries: -LLM -'large language model' -'program synthesis' -'CAD program' -'command sequence'.
- Prioritize searching within computer vision and graphics conference proceedings (CVPR, ICCV, SIGGRAPH) rather than general repositories.
